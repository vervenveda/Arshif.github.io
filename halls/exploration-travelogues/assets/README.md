# Assets

The current release is self-contained in `index.html`.

Reserve this folder for future:
- local maps;
- public-domain or licensed images;
- route diagrams;
- GIS exports;
- printables;
- rubrics;
- local data;
- CSS/JS modules.

Do not add private precise-location data or culturally restricted material simply because it is useful for a map.
