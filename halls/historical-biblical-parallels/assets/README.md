# Assets

The current Hall is self-contained in `index.html`. Reserve this directory for locally hosted maps, public-domain/licensed artifact images, diagrams, rubrics, datasets, CSS/JS modules and printables.
