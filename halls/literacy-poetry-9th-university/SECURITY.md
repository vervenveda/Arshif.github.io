# SECURITY

Static GitHub Pages cannot safely hold secrets or protected student records.

Do not commit passwords, API keys, private tokens, medical data, student PII, or unpublished work that should remain private.

The PROSE bridge uses explicit user action rather than hidden cross-origin transfer.
