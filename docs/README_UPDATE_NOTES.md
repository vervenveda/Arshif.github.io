# README UPDATE NOTES

The live repository README still describes an older ARSHIF architecture centered on a larger `apps/` inventory.

Recommended next README revision:
1. Make `halls/` a first-class directory.
2. Document the current three internal knowledge layers:
   - `halls/`
   - `apps/`
   - `Faith/apps/`
3. Replace old root-level Hall filenames with the nine canonical Hall folders.
4. Replace the old misspelled `remnant_nations_inndex.html` route with `Faith/apps/remnant_nations_index.html`.
5. Document the two repair-held fragments.
6. Document duplicate-held Testament and Truth Tribe Kids files.
7. Add the updated `mentor-manifest.json` v3 and Matrix Corridors model.
8. Keep external research gateways separate from internal ARSHIF resources.

This package does not overwrite the repository README automatically.
